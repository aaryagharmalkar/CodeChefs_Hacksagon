name: dem_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  go_router: ^14.2.0
  flutter_tts: ^4.0.0
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  permission_handler: ^12.0.1
  path_provider: ^2.1.5
  record: ^6.2.0          # ← updated from 5.2.1; all sub-packages resolve together
  http: ^1.6.0
  http_parser: ^4.0.2
  flutter_secure_storage: ^10.0.0
  cupertino_icons: ^1.0.8
  speech_to_text: ^7.3.0
  table_calendar: ^3.1.2

# ← dependency_overrides block removed entirely

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/pencil.png
    - assets/watch.jpg